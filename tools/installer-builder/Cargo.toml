[package]
name = "installer-builder"
version = "0.1.0"
edition = "2021"

[features]
bundled = []

[dependencies]
anyhow = "1.0"
rand = "0.8"
hex = "0.4"
serde_json = "1.0"
voidcore-shared = { path = "../../crates/shared" }